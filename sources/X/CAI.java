package X;

import java.util.concurrent.TimeUnit;

public abstract class CAI {
    public static final CAI $redex_init_class = null;

    static {
        TimeUnit.MICROSECONDS.convert(10, TimeUnit.MILLISECONDS);
    }
}
