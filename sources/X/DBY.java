package X;

import android.graphics.Matrix;

public abstract class DBY implements EBA {
    public static void A00(Matrix matrix, float f, float f2) {
        matrix.postTranslate((float) ((int) (f + 0.5f)), (float) ((int) (f2 + 0.5f)));
    }
}
